---
// Reusable section container with consistent vertical spacing
export interface Props {
  class?: string;
  id?: string;
  padding?: 'sm' | 'md' | 'lg' | 'xl';
  background?: 'default' | 'dark' | 'darker';
}

const {
  class: className = '',
  id,
  padding = 'md',
  background = 'default',
} = Astro.props;

const paddingClasses = {
  sm: 'py-8 sm:py-12',
  md: 'py-12 sm:py-16 md:py-20 lg:py-28',
  lg: 'py-16 sm:py-20 md:py-24 lg:py-32',
  xl: 'py-20 sm:py-24 md:py-32 lg:py-40',
};

const backgroundClasses = {
  default: '',
  dark: 'bg-zinc-900/30',
  darker: 'bg-zinc-950',
};
---

<section
  id={id}
  class={`${paddingClasses[padding]} ${backgroundClasses[background]} ${className}`}
>
  <slot />
</section>

