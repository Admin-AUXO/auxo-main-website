---
import { aboutContent } from "../../../data/content/about";

interface Props {
  content: typeof aboutContent.hero;
}

const { content } = Astro.props;
---

<div class="max-w-4xl mx-auto mb-4 sm:mb-5 md:mb-6 lg:mb-8 px-4 sm:px-0">
  <div class="text-center">
    <div
      class="inline-block px-3 py-1.5 sm:px-4 sm:py-2 bg-accent-green/10 border border-accent-green rounded-full mb-8 sm:mb-12 md:mb-16 lg:mb-20"
    >
      <span
        class="text-accent-green text-xs sm:text-sm font-semibold tracking-wide transition-colors"
        >{content.badge}</span
      >
    </div>
    <h1
      class="text-xl sm:text-2xl md:text-3xl lg:text-4xl xl:text-5xl font-bold mb-2 sm:mb-3 md:mb-4 lg:mb-6 leading-tight text-primary transition-colors"
    >
      {content.title}
      <span class="text-accent-green transition-colors">
        {content.titleHighlight}</span
      >
    </h1>
    <p
      class="text-sm sm:text-base md:text-lg text-secondary leading-relaxed transition-colors max-w-3xl mx-auto"
    >
      {content.subtitle}
    </p>
  </div>
</div>
