---
import { Icon } from "astro-icon/components";

interface Value {
  icon: string;
  title: string;
  description: string;
  demonstration?: string;
}

interface Props {
  values: Value[];
  title: string;
  subtitle: string;
}

const { values, title, subtitle } = Astro.props;
---

<div class="max-w-6xl mx-auto mb-6 sm:mb-8 md:mb-12 lg:mb-20">
  <div class="text-center mb-4 sm:mb-6 md:mb-8 lg:mb-12">
    <h2
      class="text-xl sm:text-2xl md:text-3xl font-bold mb-1.5 sm:mb-2 md:mb-3 lg:mb-4 text-primary transition-colors"
    >
      {title}
    </h2>
    <p
      class="text-xs sm:text-sm md:text-base text-secondary leading-relaxed transition-colors"
    >
      <em>{subtitle}</em>
    </p>
  </div>

  <div class="space-y-4 sm:space-y-6 md:space-y-8">
    {values.map((value) => (
      <div class="bg-card border border-theme rounded-xl p-4 sm:p-5 md:p-6 lg:p-8 hover:border-accent-green transition-all">
        <div class="flex flex-col sm:flex-row gap-3 sm:gap-4 md:gap-6">
          <div class="flex-shrink-0">
            <div class="w-12 h-12 sm:w-14 sm:h-14 md:w-16 md:h-16 bg-accent-green/10 rounded-xl flex items-center justify-center">
              <Icon
                name={value.icon}
                class="w-6 h-6 sm:w-7 sm:h-7 md:w-8 md:h-8 lg:w-10 lg:h-10 text-accent-green transition-colors"
              />
            </div>
          </div>
          <div class="flex-1">
            <h3 class="font-bold mb-1.5 sm:mb-2 text-base sm:text-lg md:text-xl text-primary transition-colors">
              {value.title}
            </h3>
            <p class="text-xs sm:text-sm md:text-base text-secondary mb-3 sm:mb-4 transition-colors leading-relaxed">
              {value.description}
            </p>
            {value.demonstration && (
              <div class="bg-surface/50 border border-accent-green/20 rounded-lg p-2.5 sm:p-3 md:p-4">
                <div class="flex items-start gap-1.5 sm:gap-2">
                  <Icon name="mdi:check-circle" class="w-4 h-4 sm:w-5 sm:h-5 text-accent-green transition-colors flex-shrink-0 mt-0.5" />
                  <div>
                    <p class="text-xs sm:text-sm font-semibold text-primary mb-1 transition-colors">
                      How we demonstrate this:
                    </p>
                    <p class="text-xs sm:text-sm text-secondary transition-colors leading-relaxed">
                      {value.demonstration}
                    </p>
                  </div>
                </div>
              </div>
            )}
          </div>
        </div>
      </div>
    ))}
  </div>
</div>

